--- a/include/linux/susfs.h
+++ b/include/linux/susfs.h
@@ -170,4 +170,23 @@
 /* susfs_init */
 void susfs_init(void);
 
+#include <linux/errno.h>
+#ifndef CMD_SUSFS_ADD_SUS_PATH_LOOP
+#define CMD_SUSFS_ADD_SUS_PATH_LOOP 0x55a0
+static inline int susfs_add_sus_path_loop(void __user *arg) { return -EOPNOTSUPP; }
+#endif
+#ifndef CMD_SUSFS_HIDE_SUS_MNTS_FOR_NON_SU_PROCS
+#define CMD_SUSFS_HIDE_SUS_MNTS_FOR_NON_SU_PROCS 0x55a1
+static inline int susfs_hide_sus_mnts_for_non_su_procs(void __user *arg) { return -EOPNOTSUPP; }
+#endif
+#ifndef CMD_SUSFS_ADD_SUS_MAP
+#define CMD_SUSFS_ADD_SUS_MAP 0x55a2
+static inline int susfs_add_sus_map(void __user *arg) { return -EOPNOTSUPP; }
+#endif
+#ifndef CMD_SUSFS_ENABLE_AVC_LOG_SPOOFING
+#define CMD_SUSFS_ENABLE_AVC_LOG_SPOOFING 0x55a3
+static inline int susfs_enable_avc_log_spoofing(void __user *arg) { return -EOPNOTSUPP; }
+#endif
+
 #endif
